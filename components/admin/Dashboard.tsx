"use client";
import Image from "next/image";
import Link from "next/link";
import { useEffect, useState } from "react";
import { getDashboard, type DashboardStats, type RecentContent } from "@/features/admin/api/dashboard";
const href: Record<string, string> = { project: "/admin/du-an", post: "/admin/tin-tuc", course: "/admin/khoa-hoc", service: "/admin/dich-vu" };
const label: Record<string, string> = { project: "Dự án", post: "Tin tức", course: "Khóa học", service: "Dịch vụ" };
export function Dashboard() { const [stats, setStats] = useState<DashboardStats | null>(null); const [recent, setRecent] = useState<RecentContent[]>([]); const [error, setError] = useState(""); useEffect(() => { void getDashboard().then(([s, r]) => { setStats(s.data); setRecent(r.data); }).catch(e => setError(e instanceof Error ? e.message : "Không thể tải dashboard")); }, []); if (error) return <section className="admin-panel admin-empty"><p>{error}</p></section>; if (!stats) return <section className="admin-panel admin-empty"><p>Đang tải dữ liệu…</p></section>; const domains = [stats.projects, stats.posts, stats.courses, stats.services]; const total = domains.reduce((n, x) => n + x.total, 0); const published = domains.reduce((n, x) => n + (x.byStatus.published ?? 0) + (x.byStatus.planned ?? 0) + (x.byStatus.in_progress ?? 0) + (x.byStatus.completed ?? 0), 0); const drafts = domains.reduce((n, x) => n + (x.byStatus.draft ?? 0), 0); const leads = stats.contacts.reduce((n, x) => n + x._count, 0) + stats.registrations.reduce((n, x) => n + x._count, 0); return <><section className="admin-stats"><article><div><span>Tổng nội dung</span><strong>{total}</strong><small>Dữ liệu PostgreSQL</small></div><i>▤</i></article><article><div><span>Đã xuất bản</span><strong>{published}</strong><small>{total ? Math.round(published / total * 100) : 0}% tổng nội dung</small></div><i>✓</i></article><article><div><span>Bản nháp</span><strong>{drafts}</strong><small>Cần hoàn thiện</small></div><i>✎</i></article><article><div><span>Liên hệ & đăng ký</span><strong>{leads}</strong><small>Dữ liệu gửi từ public site</small></div><i>↗</i></article></section><section className="admin-panel admin-recent"><header><div><h2>Nội dung gần đây</h2><p>Cập nhật thật từ cơ sở dữ liệu</p></div></header><div className="admin-table-wrap"><table><thead><tr><th>NỘI DUNG</th><th>LOẠI</th><th>TRẠNG THÁI</th><th>CẬP NHẬT</th><th/></tr></thead><tbody>{recent.map(item => <tr key={`${item.type}-${item.id}`}><td><Image src={item.image} alt="" width={54} height={38}/><span><strong>{item.title}</strong><small>/{item.slug}</small></span></td><td>{label[item.type]}</td><td><span className="admin-status">{item.status}</span></td><td>{new Intl.DateTimeFormat("vi-VN").format(new Date(item.updatedAt))}</td><td><Link href={href[item.type]}>→</Link></td></tr>)}</tbody></table></div></section></>; }
