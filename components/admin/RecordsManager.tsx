"use client";
import { useCallback, useEffect, useState } from "react";
import { adminRecordsApi, type AdminRecord, type RecordKind } from "@/features/admin/api/records";
export function RecordsManager({ kind }: { kind: RecordKind }) { const newsletter = kind === "newsletter/subscriptions"; const [items, setItems] = useState<AdminRecord[]>([]); const [search, setSearch] = useState(""); const [status, setStatus] = useState(""); const [page, setPage] = useState(1); const [pages, setPages] = useState(1); const [busy, setBusy] = useState(false); const [error, setError] = useState(""); const load = useCallback(async () => { try { const result = await adminRecordsApi.list(kind, search, status, page); setItems(result.data); setPages(result.meta.totalPages || 1); } catch (e) { setError(e instanceof Error ? e.message : "Không thể tải dữ liệu"); } }, [kind, page, search, status]); useEffect(() => { const t = window.setTimeout(() => void load(), 250); return () => clearTimeout(t); }, [load]); async function update(item: AdminRecord, value: string) { setBusy(true); try { await adminRecordsApi.update(kind, item.id, newsletter ? { isActive: value === "active" } : { status: value }); await load(); } catch (e) { setError(e instanceof Error ? e.message : "Không thể cập nhật"); } finally { setBusy(false); } } async function remove(item: AdminRecord) { if (!confirm(`Xóa bản ghi ${item.email}?`)) return; setBusy(true); try { await adminRecordsApi.remove(kind, item.id); await load(); } catch (e) { setError(e instanceof Error ? e.message : "Không thể xóa"); } finally { setBusy(false); } } return <section className="admin-panel content-panel"><div className="content-toolbar"><label><span>⌕</span><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Tìm theo tên, email..."/></label><select value={status} onChange={e => setStatus(e.target.value)}><option value="">Tất cả trạng thái</option>{newsletter ? <><option value="active">Đang hoạt động</option><option value="unsubscribed">Đã hủy</option></> : <><option value="new">Mới</option><option value="in_progress">Đang xử lý</option><option value="resolved">Đã giải quyết</option><option value="spam">Spam</option></>}</select></div>{error && <div className="homepage-feedback">{error}</div>}<div className="admin-table-wrap"><table><thead><tr><th>NGƯỜI GỬI</th><th>LIÊN HỆ</th><th>NỘI DUNG / KHÓA HỌC</th><th>TRẠNG THÁI</th><th>NGÀY</th><th/></tr></thead><tbody>{items.map(item => <tr key={item.id}><td><span><strong>{item.name ?? item.email}</strong><small>{item.company}</small></span></td><td><span>{item.email}<small>{item.phone}</small></span></td><td><span>{item.course?.title ?? item.message?.slice(0, 80) ?? "Đăng ký nhận tin"}</span></td><td><select disabled={busy} value={newsletter ? (item.isActive ? "active" : "unsubscribed") : item.status?.toLowerCase()} onChange={e => void update(item, e.target.value)}>{newsletter ? <><option value="active">Đang hoạt động</option><option value="unsubscribed">Đã hủy</option></> : <><option value="new">Mới</option><option value="in_progress">Đang xử lý</option><option value="resolved">Đã giải quyết</option><option value="spam">Spam</option></>}</select></td><td>{new Intl.DateTimeFormat("vi-VN").format(new Date(item.createdAt))}</td><td><button disabled={busy} onClick={() => void remove(item)}>⌫</button></td></tr>)}</tbody></table></div><footer className="content-footer"><button disabled={page <= 1} onClick={() => setPage(x => x - 1)}>←</button><span>Trang {page}/{pages}</span><button disabled={page >= pages} onClick={() => setPage(x => x + 1)}>→</button></footer></section>; }
