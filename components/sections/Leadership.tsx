import Image from "next/image";
import { governanceMembers } from "@/constants/site-content";

export function Leadership() {
  return <section className="w-full bg-white py-16 lg:py-24"><div className="mx-auto w-[calc(100%_-_32px)] max-w-[1440px] md:w-[calc(100%_-_48px)] lg:w-[calc(100%_-_80px)]"><header className="mb-10 grid gap-5 md:grid-cols-2 md:items-end"><div><p className="mb-3 text-xs font-semibold uppercase tracking-[.1em] text-[#087f7d]">CON NGƯỜI BIM4C</p><h2 className="text-section-title font-semibold text-[#063f46]">Hệ thống quản trị</h2></div><p className="text-[#667775] md:text-right">Một đội ngũ. Một định hướng.<br />Một chuẩn mực chất lượng.</p></header><div className="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3">{governanceMembers.map((item, index) => <article className="group relative aspect-[4/5] overflow-hidden bg-[#063f46]" key={item.title}><Image className="object-cover transition-transform duration-500 group-hover:scale-105" src={item.image} alt={item.title} fill sizes="(max-width: 767px) 100vw, (max-width: 1023px) 50vw, 33vw" /><div className="absolute inset-0 bg-gradient-to-t from-[#063f46]/95 via-transparent to-transparent" /><span className="absolute left-5 top-5 text-xs font-semibold text-[#ffffff]">0{index + 1}</span><div className="absolute inset-x-0 bottom-0 p-6 text-white"><p className="mb-2 text-xs font-semibold uppercase tracking-wider text-[#ffffff]">{item.label}</p><h3 className="text-2xl font-semibold">{item.title}</h3></div></article>)}</div></div></section>;
}
